// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['c10.patreonusercontent.com', 'cdn.britannica.com'], // Add any other domains as needed
    },
};

module.exports = nextConfig;
